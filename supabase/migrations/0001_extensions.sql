-- ============================================================
-- 0001 — الامتدادات
-- ============================================================

-- gen_random_uuid()
create extension if not exists pgcrypto;

-- ★ حرج ★
-- btree_gist يسمح بدمج مقارنة المساواة (=) على place_id مع مقارنة التداخل (&&)
-- على المدى الزمني داخل قيد استبعاد واحد. بدونه يستحيل منع الحجز المزدوج
-- على مستوى قاعدة البيانات، ويعود المنع إلى الكود حيث تظهر حالات التسابق.
create extension if not exists btree_gist;
